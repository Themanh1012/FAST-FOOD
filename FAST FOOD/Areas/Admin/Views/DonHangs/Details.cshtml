@*@model FAST_FOOD.Models.DonHang

@{
    ViewBag.Title = "Details";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
}
<div class="container mt-5">
    <div class="card shadow border-0 rounded-4">
        <div class="card-header bg-danger text-white">
            <h4><i class="fas fa-trash></i>Xóa đơn hàng</h4>
        </div>
        <div class="card-body text-center">
            <p class="text-muted fs-5">Bạn có chắc muốn xóa đơn hàng của <strong class="text-dark">@Model.TenKhachHang</strong>?</p>
            <p><strong>Tổng tiền: </strong>@string.Format("{0:N0} đ", Model.TongTien)</p>
            <p><strong>Ngày Đặt: </strong>@Model.NgayDat.ToString("dd/MM/yyyy")</p>
            <p><strong>Trạng thái: </strong>@Model.TrangThai</p>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
        <div class="d-flex justity-content-center mt-4 gap-3">
            <a href="@Url.Action("Index")" class="btn btn-secondary">
                <i class="fas fa-times"></i>Hủy
            </a>
            <button type="submit" class="btn btn-danger">
                <i class="fas fa-trash"></i> Xóa đơn hàng
            </button>
        </div>
            }
        </div>
    </div>
</div>*@