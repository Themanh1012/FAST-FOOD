@model IEnumerable<FAST_FOOD.Models.HinhThucThanhToan>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenHinhThuc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenHinhThuc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaHTTT }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaHTTT }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaHTTT })
        </td>
    </tr>
}

</table>
